
import React from 'react';

// This page has been disabled for security reasons.
// Admin functionality is removed from the client-side build.

interface AdminPageProps {
  isOpen: boolean;
  onClose: () => void;
  // Other props are irrelevant now
}

const AdminPage: React.FC<any> = (props) => {
    return null;
};

export default AdminPage;
